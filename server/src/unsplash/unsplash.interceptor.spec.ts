import { UnsplashInterceptor } from './unsplash.interceptor';

describe('UnsplashInterceptor', () => {
  it('should be defined', () => {
    expect(new UnsplashInterceptor()).toBeDefined();
  });
});
