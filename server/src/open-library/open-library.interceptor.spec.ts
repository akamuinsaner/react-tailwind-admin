import { OpenLibraryInterceptor } from './open-library.interceptor';

describe('OpenLibraryInterceptor', () => {
  it('should be defined', () => {
    expect(new OpenLibraryInterceptor()).toBeDefined();
  });
});
