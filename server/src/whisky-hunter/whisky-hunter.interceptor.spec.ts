import { WhiskyHunterInterceptor } from './whisky-hunter.interceptor';

describe('WhiskyHunterInterceptor', () => {
  it('should be defined', () => {
    expect(new WhiskyHunterInterceptor()).toBeDefined();
  });
});
